//var num=Math.floor(Math.random()*4)+1;

$('#card').wScratchPad({
    size        : 10,          // The size of the brush/scratch.
    bg          : 'images/sam3.jpg',  // Background (image path or hex color).
    fg          :  'images/sam1.jpg',  // Foreground (image path or hex color).
   
    cursor      : 'pointer' // Set cursor.
  });